export type CartLineItem = {
  variantId: string;
  productId: string;
  productSlug: string;
  productName: string;
  imageUrl: string;
  quantity: number;
  size: string;
  color: string;
  unitPriceCents: number;
};
